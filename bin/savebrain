#!/bin/bash

fecha=$(date +"%Y-%m-%d")

mensaje="backup: notas actualizadas [$fecha]"

cd $HOME/Documentos/segundo_cerebro || exit

git add . 

git commit -m "$mensaje"

git push

echo "$mensaje enviado al repositorio"
