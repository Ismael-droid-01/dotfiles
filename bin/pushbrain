#!/bin/bash

fecha=$(date +"%Y-%m-%d")

mensaje="backup(PC): notas actualizadas [$fecha]"

cd /media/samuel/segcerebro || exit

git add . 

git commit -m "$mensaje"

git push

echo "$mensaje enviado al repositorio"
